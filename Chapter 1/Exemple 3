#include <stdio.h>

int main(void) {
  /*Escreva um programa que verifique a validade de uma senha fornecida pelo
  usuário. A senha válida é o número 1234. Devem ser impressas as seguintes
  mensagens:
  ACESSO PERMITIDO caso a senha seja válida.
  ACESSO NEGADO caso a senha seja inválida.*/

  int entrada;
  printf("Digite a senha: "); scanf("%i",& entrada);

  if (entrada==1234){printf("Senha correta! ");
  } else {
  printf("Erro! Senha incorreta");
  }


  return 0;
}
