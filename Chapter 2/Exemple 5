#include <stdio.h>

int main(void) {


  /*Para que a divisão entre 2 números possa ser realizada, o divisor não pode ser nulo
  (zero). Escreva um programa para ler 2 valores e imprimir o resultado da divisão do
  primeiro pelo segundo. OBS: O programa deve validar a leitura do segundo valor (que
  não deve ser nulo). Enquanto for fornecido um valor nulo a leitura deve ser repetida.
  Utilize a estrutura de repetição na construção da repetição de validação.*/
  
  

  for ( int i = 0; i<10000; i++){
    float num1, num2;
    printf("\n\nDigite o dividendo: "); scanf("%f", & num1);
    printf("Digite o divisor: "); scanf("%f", & num2);
    float divisao = num1/num2;
    printf("%.2f", divisao);
      if(num2==0){
      printf(" Não é possível ter um divisor nulo!!");
      break;
      }
    }
  
  return 0;
}
